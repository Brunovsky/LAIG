var DEGREE_TO_RAD = Math.PI / 180;

var AXIS_THICKNESS = 0.05;

var ENABLED_LIGHTS_VISIBLE = true;

var DISABLED_LIGHTS_VISIBLE = false;

var DEBUG_DISPLAY = true;

var DEFAULT_YAS_FILE = "scenes/trialscene.xml";

var LIGHT_CONSTANT_ATTENUATION = 0.5;

var LIGHT_LINEAR_ATTENUATION = 0.01;

var LIGHT_QUADRATIC_ATTENUATION = 0.0025;

function degToRad(deg) {
    return deg * DEGREE_TO_RAD;
}
